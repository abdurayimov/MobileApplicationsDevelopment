<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nazim.a7_laba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MultiScreenMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Canari"
            android:label="@string/can" />
        <activity
            android:name=".Curili"
            android:label="@string/title_activity_curili" />
        <activity
            android:name=".Maldivi"
            android:label="@string/title_activity_maldivi" />
        <activity
            android:name=".Philiphini"
            android:label="@string/title_activity_philiphini"></activity>
    </application>

</manifest>